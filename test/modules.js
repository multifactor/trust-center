/* eslint no-unused-expressions: "off" */
require('chai').should()
// const trust-center = require('../src')
const { suite, test } = require('mocha')

suite('index', () => {
  test('module1', async () => {

  })
})
